# https://docs.spring.io/spring-boot/docs/current/reference/html/appendix-application-properties.html
server.port=3001
server.servlet.context-path=/course-api
spring.jpa.hibernate.ddl-auto=update

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.beans.enabled=true
management.endpoint.metrics.enabled=true

logging.file.name=spring-boot-log.log

course-api.settings.enabled=true

# Spring security uses the below user details for authentication by default
#spring.security.user.name=aara
#spring.security.user.password=123